<!DOCTYPE html>

<html lang='en'>
  <head>
    <title>TinyApp</title>
  </head>

  <header>
  <% include partials/_header %>
  </header>

  <body>
    <ul>
    <% for(var url in urls) { %>
      <li><%= urls[url] + ":    " + url %></li>
    <% } %>
    </ul>
    <a href=https://joshuadennis.ca>Create a New Shortened URL</a>
  </body>
  </html>
